const express = require("express")
const router = new express.Router()
const bcrypt = require('bcrypt')
const User = require('../models/model.user')

//GET to log in  + signup

module.exports = router;